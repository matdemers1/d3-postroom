{
  "name": "@postroom/dav-proto",
  "private": true,
  "version": "0.0.0",
  "description": "WebDAV/CalDAV/CardDAV wire protocol: a strict, XXE-proof XML parser and serializer for the DAV namespace subset, multistatus, request bodies and HTTP precondition helpers",
  "type": "module",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --passWithNoTests",
    "test:integration": "vitest run --project integration --passWithNoTests"
  },
  "dependencies": {},
  "devDependencies": {}
}
