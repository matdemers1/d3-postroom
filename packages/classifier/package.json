{
  "name": "@postroom/classifier",
  "private": true,
  "version": "0.0.0",
  "description": "Signal extraction and sorting for the self-sorting inbox — every decision stores its reasons (PST-ADR-007)",
  "type": "module",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --project unit --passWithNoTests && vitest run --config test/golden/vitest.config.ts --passWithNoTests",
    "test:integration": "vitest run --project integration --passWithNoTests"
  },
  "dependencies": {
    "@postroom/mime": "workspace:*"
  },
  "devDependencies": {}
}
