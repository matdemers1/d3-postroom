// HTTP API and web login: dual login, admin, setup.
// Placeholder entrypoint until its phase lands; the image dispatches `postroom api` here.
import { describeDaemon } from './daemon.js';

console.log(describeDaemon());
