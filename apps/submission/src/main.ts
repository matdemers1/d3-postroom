// Authenticated submission on :465/:587.
// Placeholder entrypoint until its phase lands; the image dispatches `postroom submission` here.
import { describeDaemon } from './daemon.js';

console.log(describeDaemon());
